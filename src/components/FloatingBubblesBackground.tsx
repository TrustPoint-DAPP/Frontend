import React, { useRef } from "react";

export default function FloatingBubblesBackground() {
  const containerRef = useRef() as React.MutableRefObject<HTMLInputElement>;

  return <div>FloatingBubblesBackground</div>;
}
